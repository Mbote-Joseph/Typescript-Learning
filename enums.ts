enum SeatChoice {
  AISLE,
  MIDDLE,
  WINDOW,
  FOURTH,
}

enum SeatChoices {
  AISLE = 10,
  MIDDLE = 22,
  WINDOW,
  FOURTH = 44,
}
const hcSeat = SeatChoice.MIDDLE;

SeatChoice;

console.log(hcSeat);

export {};
