enum SeatChoice {
  AISLE,
  MIDDLE,
  WINDOW,
  FOURTH,
}

const enum SeatChoices {
  AISLE = 10,
  MIDDLE = 22,
  WINDOW,
  FOURTH = 44,
  NEXT,
}
const hcSeat = SeatChoice.MIDDLE;

console.log(hcSeat);

export {};
