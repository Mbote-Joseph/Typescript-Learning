let message: string = "Hello Joseph!";

console.log(message);

let user = { name: "Joseph", email: "mbotejoseph001@gmail.com", age: 25 };

console.log(
  `The following are the users details: ${user.name} and ${user.email} and ${user.age}`
);
